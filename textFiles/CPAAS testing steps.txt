1. Open Git bash terminal

2.Login to the open shift project by giving below

oc login --server=https://api.di1001.cpaas.test:6443 -u vadlapasadmin -p Augadmin@2022 --insecure-skip-tls-verify=true
oc project fenergo-dev-di1001

3.Check pods are running or not by giving "oc get pods"

4.Scale up or down the server pods based up on your requirement in open shift
  open shift url :https://console.apps.di1001.cpaas.test/

5.Copy Jmeter, parameter files to deploy folder. By ruuning copy cmds, we can these files to pods

  copy cmds :oc cp LoadTest2hrs_R6.0.jmx $MASTER_NAME:/jmeter/apache-jmeter-5.4.2 -c distributed-jmeter (To cpy jmx)
             oc cp users_test12.txt $server1:/jmeter/apache-jmeter-5.4.2 -c distributed-jmeter (to cpy text file)
note:
Everytime we need to change the names of server and master after scaling

6.Run the alpaca file in the terminal of master and slave by giving the below cmd

  Alpaca cmd:./alpaca_v1.3.1_linux-amd64 -d GLOBALTEST -u vadlapasadmin -C http://tlpaclb-qa.intranetlb-qa.anz:9001/proxy.pac

7.Start the test by executing the below cmd

  Execution Cmd: oc exec -it $MASTER_NAME -- jmeter -n -t LoadTest2hrs_R6.0.jmx -l loadtest25Aug2hr.jtl -Dserver.rmi.ssl.disable=true -R $SERVER_IPS

8.To cpy the jtl file which came after the test run from pods to deploy folder of VM.

  cmd: oc rsync $MASTER_NAME:/jmeter/apache-jmeter-5.4.2/loadtest25Aug2hr.jtl .

*****************FYI************
To cpy the filter files to bin:

oc cp FilterResults.sh $MASTER_NAME:/jmeter/apache-jmeter-5.4.2/bin -c distributed-jmeter
oc cp FilterResults.bat $MASTER_NAME:/jmeter/apache-jmeter-5.4.2/bin -c distributed-jmeter

*****IMP cmds visual stdio*******

oc get pods  :To see the pods in the visual stdio
cd deploy    :To go to deploy path
To stopt the test: ctrl +c
To go back to path: cd ..

*******IMP cmds in pods terminal**

To Check current path  : pwd
To check what is there inside the path :ls   
To check How many are there in that path: ls -ltr  








